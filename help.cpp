
void help()
{   
       cout << "The purpose of this fxn is to extract the colors of an image ju    dging from the Mat object. The output should be a dictionary of colors in wo    rds. Ex: [red, blue, green, etc]"<< endl;
	        //cout << "This fxn takes 1 argument. That argument being an image." <<     endl;
	         cout << "First use 'cmake .', then 'make', then call the fxn using the f    ormat below" << endl;
		      cout << "To call this fxn use the format: " << endl;
		       cout << "./mat image_name.png" << endl << endl << endl;
		    }

